rootProject.name = "test-ktor-kotlin"

/*
dependencyResolutionManagement {
  versionCatalogs {
    // declares a catalog, named 'libs', from the 'libs.versions.toml' file
    create("libs") {
      from(files("./gradle/libs.versions.toml"))
    }
  }
}
*/

